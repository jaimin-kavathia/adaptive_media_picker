name: adaptive_media_picker
description: "Adaptive media picker for Flutter with smart permissions and limited-access UI."
version: 0.0.1
repository: https://github.com/logicrays/adaptive_media_picker

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^11.5.0
  flutter_screenutil: ^5.9.3
  image_picker: ^1.2.0
  permission_handler: ^12.0.1
  photo_manager: ^3.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
